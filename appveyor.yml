# version format
version: v{build}

branches:
  only:
    - master

# Skipping commits with particular message or from specific user
skip_commits:
  message: "*[skip ci]*"

# Start builds on tags only
skip_non_tags: true

image: Visual Studio 2019

# scripts that are called at very beginning, before repo cloning
init:
  - git config --global core.autocrlf input

matrix:
  fast_finish: true     # set this flag to immediately finish build once one of the jobs fails.

before_build:
  - nuget restore ZombifyMe.sln

build:
  project: build_all.xml

after_build:
  - dotnet pack --configuration Release /p:Platform=x64 ZombifyMe/ZombifyMe.csproj

test: off

artifacts:
  - path: ZombifyMe/bin/
    name: ZombifyMe
  - path: ZombifyMe/bin/x64/Release/ZombifyMe.*.nupkg
    name: ZombifyMe-Package

deploy:
  - provider: GitHub
    release: '%APPVEYOR_REPO_TAG_NAME%'
    description: '%APPVEYOR_REPO_COMMIT_MESSAGE%'
    auth_token:
      secure: dUpQSnUT5/m88z1BqHNnRjaxxZzz09CmyqFJr/cAyWjqKeLgnKunlIZlEKAXlNIm
    artifact: ZombifyMe-Release
    draft: true
    prerelease: false
    on:
      branch: master
  - provider: NuGet
    server: https://nuget.pkg.github.com/dlebansais/index.json
    artifact: ZombifyMe-Package
    username: dlebansais
    api_key:
      secure: GOksqNi8I7Vs6dcsfosJvSa9vMaZH3zLQJjnx+ruJy6ZeLF075mifj4MdRI34E8W
    skip_symbols: true
